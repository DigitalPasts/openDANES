{
  "name": "opendanes",
  "version": "1.0.0",
  "description": "This is the repository which holds the github pages website for the openDANES platform, a website for publishing tutorials and white papers about using computational methodologies for ancient Near Eastern studies.",
  "main": "index.js",
  "dependencies": {
    "fuzzysearch": "^1.0.3",
    "simple-jekyll-search": "^1.10.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bundle exec jekyll build && npx pagefind --site _site",
    "serve": "npm run build && bundle exec jekyll serve --skip-initial-build",
    "pdf": "node assets/js/generate-pdf.js",
    "pdf:pages": "node assets/js/generate-pdf.js --all-pages",
    "pdf:newsletters": "node assets/js/generate-pdf.js --all-newsletters",
    "slugs": "node assets/js/generate-slugs.js",
    "slugs:apply": "node assets/js/generate-slugs.js --apply"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}